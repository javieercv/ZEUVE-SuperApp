public enum OrganizerStorageKeys {
    public static let defaultOptions = "organizer.defaultOptions"
    public static let lastFolder = "organizer.lastFolder"
    public static let recentFolders = "organizer.recentFolders"

    public enum Legacy {
        public static let options = "organizer.options"
    }
}
